FROM python
# берём из докер-хаба образ python (дефолт)

# COPY копирует файлы из нашей директории в свой образ
# из корня        # в какую папку в образе
# COPY .                  /app

WORKDIR /app
# тогда можно писать (где . будет относительный путь относительно WORKDIR)

COPY . . 
# из корня в /app/.

# RUN запускается однажды - только при создании образа
# RUN pip install

# показываем какой порт будет "слушать" через докер-контейнер
EXPOSE 3000

# CMD запускается каждый раз при запуске контейнера
CMD ["python", "py.py"]





# запустить уже созданный контейнер после docker stop:
# docker start <ID>

# у докер run есть много флагов
# например
# docker run -p 3000:3000 <ID>
# первый порт говорит о локальном порте на локальной машине
# второй - внутри докер-контейнера